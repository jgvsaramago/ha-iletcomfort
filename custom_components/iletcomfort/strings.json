{
  "config": {
    "step": {
      "user": {
        "title": "iLetComfort Heat Pump",
        "description": "Enter your iLetComfort / Dollin cloud credentials and pick the region your account is registered in.",
        "data": {
          "email": "Email",
          "password": "Password",
          "region": "Region"
        }
      },
      "device": {
        "title": "Select device",
        "description": "Choose the appliance to add. Re-run the integration to add additional devices on the same account.",
        "data": {
          "appliance_code": "Device"
        }
      }
    },
    "error": {
      "invalid_auth": "Invalid email or password.",
      "cannot_connect": "Unable to connect to the Dollin cloud API. If your account is registered in Europe, switch the Region to Europe. Check Home Assistant logs for details.",
      "no_devices": "Login succeeded but no devices were found on this account.",
      "unknown": "An unexpected error occurred."
    },
    "abort": {
      "already_configured": "This device is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "iLetComfort options",
        "description": "Tune how often Home Assistant polls the cloud and which extra data it fetches, to trade freshness against API call volume.",
        "data": {
          "scan_interval": "Poll interval (seconds)",
          "fetch_diagnostics": "Fetch diagnostic sensor data",
          "fetch_schedule": "Fetch daily schedule (Configuration section)"
        },
        "data_description": {
          "scan_interval": "How often to poll the cloud, in seconds (minimum 30). Lower values react faster but use more API calls.",
          "fetch_diagnostics": "Aquapura Split Green only: also fetches the outdoor ambient / ODU sensor frame, one extra cloud command per poll. Disabling it saves that call, but the Outdoor Ambient Temperature sensor stops updating. No effect on other models — their diagnostic sensors already come from the status/sensors calls made every poll regardless.",
          "fetch_schedule": "Aquapura Split Green only: fetches the daily-schedule frame, one extra cloud command per poll, feeding the 20 'Daily Schedule' entities (Configuration section). Disabling it saves that call and removes those entities from the device (reload required); re-enabling adds them back. No effect on other models."
        }
      }
    }
  },
  "selector": {
    "region": {
      "options": {
        "us": "United States",
        "eu": "Europe"
      }
    }
  },
  "issues": {
    "device_offline": {
      "title": "Heat pump appears offline",
      "description": "Home Assistant has been receiving empty responses from device `{appliance_code}` for several minutes, so entities are showing the last known values. This usually means the heat pump has lost its connection to the iLetComfort cloud, or has been powered off. Check the device in the official iLetComfort / BTRI mobile app — if it shows as offline there too, the issue is on the device or vendor cloud side, not Home Assistant. The card will clear automatically once the device starts responding again."
    }
  }
}
